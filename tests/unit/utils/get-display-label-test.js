import getDisplayLabel from 'livarava-framework/utils/get-display-label';
import { module, test } from 'qunit';

module('Unit | Utility | get-display-label', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = getDisplayLabel();
    assert.ok(result);
  });
});
