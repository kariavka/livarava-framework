import getApiURL from 'nvdc/utils/get-api-url';
import { module, test } from 'qunit';

module('Unit | Utility | get api url');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getApiURL();
  assert.ok(result);
});
