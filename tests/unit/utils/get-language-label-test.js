import getLanguageLabel from 'livarava-framework/utils/get-language-label';
import { module, test } from 'qunit';

module('Unit | Utility | get-language-label', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = getLanguageLabel();
    assert.ok(result);
  });
});
